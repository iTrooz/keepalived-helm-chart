repositories:
  - name: stable
    url: https://charts.helm.sh/stable

releases:
  - name: keepalived-me
    namespace: keepalived-me
    chart: ./
    values:
      - values.yaml
      - values.local.yaml
